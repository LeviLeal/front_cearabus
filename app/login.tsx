import { Text } from "@react-navigation/elements"
import { View } from "react-native"

export default function Login () {
    return (
        <View>
            <Text>Logar no CearaBus</Text>
        </View>
    )
}